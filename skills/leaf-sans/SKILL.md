---
name: leaf-sans
description: Create an isolated worktree branch with its own database branch, investigate thoroughly, implement changes and push — cut from local main, or off the current worktree when run from inside one. 
---

# leaf-sans

This is a shorthand for using `/leaf`, except no need to write a fork document. Probably because the work is too small to justify it.